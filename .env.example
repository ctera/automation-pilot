# vSphere / VmTools credentials (domain account)
VMTOOLS_USER=youruser@ctera.com
VMTOOLS_PASSWORD=

# Jenkins API access
JENKINS_USER=
JENKINS_TOKEN=

# SMB share access uses the same domain creds as VMTOOLS_* above.
# No separate variable needed — sync-vmtools.sh derives SMB creds from VMTOOLS_*.
